# 범용 여행 계획 하네스

이 프로젝트에서 여행 조사·설계·변경 요청을 받으면 `harness/travel-planner/adapters/codex.md`를 읽고 `harness/travel-planner/SKILL.md`를 실행한다. 하네스 개발 요청에는 여행자 정보를 요구하지 않는다.

사용자는 총괄과 전문 서브에이전트 구성을 요청했다. 독립 조사가 필요한 경우 연결부에서 확인한 실제 서브에이전트 도구로 위임한다. 도구가 없으면 순차 역할 실행으로 전환하고 독립 검증을 수행하지 못했다는 사실을 표시한다. 역할을 설명한 것만으로 실행했다고 하지 않는다.

총괄만 `runs/`의 통합 상태와 최종 결과를 수정한다. 조사자는 읽기 전용으로 조사 결과를 반환한다. 산출물 제작자는 총괄이 지정한 `runs/<여행ID>/staging/<작업ID>/` 안에서만 제작 파일을 작성한다. 원본 계획·조사·감사·상태는 수정하지 않는다. 총괄이 검수한 파일만 최종 산출물로 등록한다. 기본 모델 설정을 상속한다. 예약·결제·외부 메시지 전송은 별도 명시적 요청이 있어야 한다.
